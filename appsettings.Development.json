{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Can be more verbose for development if needed (e.g., "Debug")
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Development Connection String (using local tunnel)
    "DefaultConnection": "server=127.0.0.1,5555;Database=sp25_3342_tuo53004;User id=tuo53004;Password=thaekee4Aize;Encrypt=False;TrustServerCertificate=True;"
    // Added Encrypt=False and TrustServerCertificate=True, often needed for local tunnels/instances
  }
  // SmtpSettings are inherited from appsettings.json.
  // The Password should be stored in User Secrets for development.
}
